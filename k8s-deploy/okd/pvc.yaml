apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: lightrag-data
  namespace: lightrag-prod
  labels:
    app: lightrag
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  # Uncomment and set your OKD storage class
  # storageClassName: standard
